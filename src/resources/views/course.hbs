<div>
    <h1>Detail Course</h1>
    <div class='row'>
        <div class='col-4'>
            <img src={{course.image}} class='img-fluid' alt='...' />
            <p>{{course.name}}</p>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>